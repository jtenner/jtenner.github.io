---
interface Props {
  compact?: boolean;
}

const { compact = false } = Astro.props;
---

<div
  class:list={[
    "bg-surface-container-low border border-outline-variant/40",
    compact ? "rounded-lg px-5 py-4 sm:px-6 sm:py-5" : "rounded-xl p-6 sm:p-8 lg:p-10",
  ]}
>
  <div class:list={[compact ? "flex flex-col gap-3" : "flex flex-col gap-6"]}>
    <div>
      <p
        class:list={[
          "font-headline font-black text-on-surface tracking-tighter",
          compact ? "text-lg" : "text-2xl",
        ]}
      >
        Joshua Tenner
      </p>
      <p
        class:list={[
          "text-on-surface-variant font-body",
          compact ? "text-xs mt-1" : "text-sm mt-2",
        ]}
      >
        Software developer building compilers and WebAssembly tools
      </p>
      {!compact && (
        <p class="text-outline text-xs mt-1 font-headline tracking-wider">
          Dad of three
        </p>
      )}
    </div>
    <div class="flex items-center gap-4">
      <a
        aria-label="Joshua Tenner on LinkedIn (opens in new tab)"
        class="text-outline hover:text-primary transition-colors"
        href="https://www.linkedin.com/in/joshua-tenner-68316167/"
        target="_blank"
        rel="noopener noreferrer"
      >
        <span aria-hidden="true" class="material-symbols-outlined text-lg">link</span>
      </a>
      <a
        aria-label="Joshua Tenner on GitHub (opens in new tab)"
        class="text-outline hover:text-primary transition-colors"
        href="https://github.com/jtenner/"
        target="_blank"
        rel="noopener noreferrer"
      >
        <span aria-hidden="true" class="material-symbols-outlined text-lg">terminal</span>
      </a>
    </div>
  </div>
</div>
