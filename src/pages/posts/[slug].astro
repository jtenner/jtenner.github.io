---
import EditorialLayout from "../../layouts/EditorialLayout.astro";
import { type CollectionEntry, getCollection } from "astro:content";
import NavHeader from "../../components/NavHeader.astro";
import Footer from "../../components/Footer.astro";
import AuthorBio from "../../components/AuthorBio.astro";
import PostCard from "../../components/PostCard.astro";
import {
  BLOG_IDENTITY,
  buildBlogPostingSchema,
  buildBreadcrumbListSchema,
  type JsonLdPayload,
} from "../../lib/structuredData";

export async function getStaticPaths() {
  const posts = await getCollection("posts");
  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props as { post: CollectionEntry<"posts"> };
const { Content } = await post.render();

const site = Astro.site ?? Astro.url;
const jsonLd = [
  buildBlogPostingSchema({
    site,
    post,
    publisher: BLOG_IDENTITY.publisher,
  }),
  buildBreadcrumbListSchema({
    site,
    postTitle: post.data.title,
    postSlug: post.slug,
  }),
].filter((schema): schema is JsonLdPayload => Boolean(schema));

const articleTitleId = `article-title-${post.slug.replace(/[^a-z0-9-]/gi, "-")}`;
const articleMetaId = `article-meta-${post.slug.replace(/[^a-z0-9-]/gi, "-")}`;
const relatedHeadingId = `related-posts-${post.slug.replace(/[^a-z0-9-]/gi, "-")}`;
const published = new Intl.DateTimeFormat("en-US", {
  month: "short",
  day: "numeric",
  year: "numeric",
}).format(post.data.pubDate);

const allPosts = (await getCollection("posts")).sort(
  (a, b) =>
    new Date(b.data.pubDate).valueOf() - new Date(a.data.pubDate).valueOf(),
);
const relatedPosts = allPosts.filter((p) => p.slug !== post.slug).slice(0, 3);

const categories = post.data.categories ?? [];
---

<EditorialLayout
  title={`${post.data.title} | Assembling Me`}
  description={post.data.description}
  jsonLd={jsonLd}
  openGraphType="article"
>
  <div
    class="fixed top-0 left-0 w-full h-[2px] bg-primary-container z-[60]"
    id="reading-progress-container"
    role="progressbar"
    aria-label="Reading progress"
    aria-valuenow="0"
    aria-valuemin="0"
    aria-valuemax="100"
  >
    <div
      class="h-full bg-primary transition-[width] duration-75 ease-linear"
      id="reading-progress-bar"
      style="width: 0%"
    ></div>
  </div>

  <NavHeader activePage="article" />

  <main role="main" class="pt-24 sm:pt-28 pb-16 sm:pb-20" id="main-content">
    <div class="max-w-screen-2xl mx-auto px-5 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-10 lg:gap-16">
        <div class="lg:col-span-9 min-w-0">
          <header class="mb-10 sm:mb-14 max-w-3xl">
            <div class="flex flex-wrap items-center gap-2 sm:gap-3 mb-5 sm:mb-6" aria-label="Categories">
              {categories.map((cat) => (
                <span
                  class="px-2.5 sm:px-3 py-1 text-[9px] sm:text-[10px] font-headline font-black uppercase tracking-[0.18em] sm:tracking-[0.2em] text-on-surface-variant bg-surface-container-low border border-outline-variant/45 rounded"
                >
                  {cat}
                </span>
              ))}
            </div>

            <h1
              id={articleTitleId}
              class="text-[2rem] sm:text-5xl md:text-6xl lg:text-7xl font-black font-headline tracking-[-0.035em] text-on-surface leading-[1.04] sm:leading-[0.98] mb-6 sm:mb-8 break-words"
            >
              {post.data.title}
            </h1>

            <div
              id={articleMetaId}
              class="flex flex-wrap items-center gap-x-3 gap-y-2 text-xs sm:text-sm text-outline font-headline tracking-wide sm:tracking-wider"
            >
              <a
                class="hover:text-primary transition-colors font-bold"
                href={`mailto:${post.data.authorEmail}`}
              >
                {post.data.author}
              </a>
              <span class="text-outline-variant" aria-hidden="true">•</span>
              <time datetime={String(post.data.pubDate)}>{published}</time>
            </div>
          </header>

          <article
            aria-labelledby={articleTitleId}
            aria-describedby={articleMetaId}
            class="prose prose-invert max-w-3xl text-[17px] sm:text-lg [&_p]:my-5 sm:[&_p]:my-6 [&_p]:leading-[1.75]"
          >
            <Content />
          </article>

          <div class="mt-14 sm:mt-20 max-w-3xl">
            <AuthorBio />
          </div>

          {relatedPosts.length > 0 && (
            <section aria-labelledby={relatedHeadingId} class="mt-16 sm:mt-24 max-w-full">
              <h2
                id={relatedHeadingId}
                class="text-2xl sm:text-3xl font-black font-headline tracking-tighter text-on-surface mb-7 sm:mb-10"
              >
                Continue Reading
              </h2>
              <div class="grid grid-cols-1 md:grid-cols-3 gap-5 sm:gap-6">
                {relatedPosts.map((rp, i) => <PostCard post={rp} index={i} />)}
              </div>
            </section>
          )}
        </div>

        <aside class="hidden lg:block lg:col-span-3">
          <div class="sticky top-36">
            <p
              class="text-[10px] font-black uppercase tracking-[0.25em] text-primary mb-7 font-headline"
            >
              On this page
            </p>
            <nav id="toc-nav" aria-label="Article sections" aria-live="polite" class="flex flex-col gap-3 text-sm font-headline">
            </nav>
          </div>
        </aside>
      </div>
    </div>
  </main>

  <Footer />

  <script is:inline>
    const progressBar = document.getElementById("reading-progress-bar");
    const progressContainer = document.getElementById("reading-progress-container");
    if (progressBar && progressContainer) {
      const updateProgress = () => {
        const scrollTop = window.scrollY || document.documentElement.scrollTop;
        const docHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
        const pct = docHeight > 0 ? (scrollTop / docHeight) * 100 : 0;
        const value = Math.min(100, Math.round(pct));
        progressBar.style.width = value + "%";
        progressContainer.setAttribute("aria-valuenow", String(value));
      };
      window.addEventListener("scroll", updateProgress, { passive: true });
      updateProgress();
    }

    const article = document.querySelector("article.prose");
    const tocNav = document.getElementById("toc-nav");
    if (article && tocNav) {
      const headings = article.querySelectorAll("h2, h3");
      headings.forEach((heading) => {
        const id = heading.id || heading.textContent?.toLowerCase().replace(/\s+/g, "-").replace(/[^\w-]/g, "");
        if (id) heading.id = id;
        const a = document.createElement("a");
        a.href = "#" + (heading.id || "");
        a.textContent = heading.textContent;
        a.className = "text-on-surface-variant hover:text-primary pl-4 py-0.5 border-l-2 border-outline-variant/40 hover:border-primary transition-all text-xs";
        tocNav.appendChild(a);
      });

      const tocLinks = [...tocNav.querySelectorAll("a")];
      const highlightToc = () => {
        let current = "";
        headings.forEach((h) => {
          const top = h.getBoundingClientRect().top;
          if (top < 120) current = h.id;
        });
        tocLinks.forEach((link) => {
          const isActive = link.getAttribute("href") === "#" + current;
          link.classList.toggle("text-primary", isActive);
          link.classList.toggle("border-primary", isActive);
          link.classList.toggle("font-bold", isActive);
          link.classList.toggle("text-on-surface-variant", !isActive);
          link.classList.toggle("border-outline-variant/40", !isActive);
          if (isActive) {
            link.setAttribute("aria-current", "location");
          } else {
            link.removeAttribute("aria-current");
          }
        });
      };
      window.addEventListener("scroll", highlightToc, { passive: true });
      highlightToc();
    }
  </script>
</EditorialLayout>
